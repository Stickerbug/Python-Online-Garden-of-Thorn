# 反馈中心 & 开发表格14 处理计划（2026-09-11）

## 数据来源

* **反馈中心**（Mojira，线上库 `/var/lib/gtn/gtn.sqlite3` 的 `public_issues` 表）：
  共 79 条；未处理**漏洞 14 条**（new 3 / needs_info 2 / confirmed 9）、
  待在办**建议 7 条**（new 6 / under_review 1）；另有已决定未完成的建议 5 条（accepted 4 / planned 1）、
  已结（fixed 38 / by_design 4 / invalid 3 / unreproducible 2 / duplicate 1 / rejected 2）。
  只读列出命令：`python scripts/list_open_feedback.py`（本表按 kind/status 自行查询）。
* **开发表格14**：`开发表格/Garden of Thorn 卡牌数据14.xlsx`（2026-09-10 更新，15 个 sheet）。
  表格用**红色填充**标"本次改动/待办"，**红字**标"取值调整"。本次提取到：

  | sheet | 红填充 | 红字 |
  | --- | --- | --- |
  | 爬塔卡牌设计 | 4 | 30（25 个"类型"、5 个"标签"） |
  | 爬塔天赋设计 | 7 | — |
  | 爬塔事件设计 | 3 | — |
  | 爬塔怪物设计 | 1 | — |
  | 战斗列表 | 2 | — |
  | 全模组卡牌 | 82 | — |

## A. 未处理漏洞（14 条）

每条先给"怎么验证"，确认后再改。带 ★ 的与故事模式/表格14 直接相关。

| ID | 状态 | 标题 | 验证方法 |
| --- | --- | --- | --- |
| 79 | new | 从天梯离开进主页后，设置里选不了娱乐模组 | 登录→打一局天梯→退到主页→设置→看娱乐模组选择；对照 `disabled_mods`/模式状态是否残留 |
| 78 | new | 额外打出的多张复制先全进手牌再依次打出，导致爆牌 | 单人训练场清空手牌、10E、打"裂变10 的松果"；跟 `auto_play_zone_top`/`copy_card_instance` 的排队顺序 |
| 76 ★ | confirmed | 故事模式里设置界面打不开 | 进故事模式→点设置；看控制台报错与 `story_*` UI 的层级/事件绑定（已排除缓存与设备） |
| 75 | confirmed | 磁铁偷取唯一卡时，手上有同名唯一卡不会变虚空 | 构造：自己手上有 X，磁铁偷对手的 X；查唯一卡处理分支 |
| 74 ★ | confirmed | 故事模式 ygg（附魔书）未触发复活 | 复现描述已很具体（24H、51 护盾、boss 56 伤害、60 毒、附魔书含 ygg）；查附魔书触发点与"死亡结算顺序" |
| 73 ★ | confirmed | 机械老鼠的战斗列表更新（"之前太难了"） | 与表格14「战斗列表」红标（工厂精英＝砖堆×1＋机械老鼠）核对，判断是"已改待确认"还是"待改" |
| 72 | confirmed | 经典 UI 无法触发装备（棍母） | 把 UI 切到经典皮肤，装备触发类卡（棍母）打出后看是否触发 |
| 71 | confirmed | 扇子对自己使用不回费 | 扇子给别人可回费、给自己不行；查自身目标分支 |
| 68 ★ | confirmed | 故事模式灌木丛每回合必刷萤火虫 | 多次进入灌木丛遭遇，统计刷怪；查遭遇/敌人权重表 |
| 64 | confirmed | 多人模式战斗日志部分状态未翻译（如 `magic_nazar`） | 触发日志后用 `grep magic_nazar` 查 i18n 表缺项 |
| 63 ★ | new | 故事模式核弹不受威力加成 | 拿威力 buff 打核弹比伤害；查"核弹"伤害是否绕过威力乘区 |
| 60 | needs_info | 护身符/火把打出时选牌后仍提示"请选择1张牌"（偶发） | 需要录像或复现条件；先查这两张牌的 `request_card` 参数与自动结算 |
| 52 ★ | confirmed | 一次性附魔（如锋利）使用后不清除 | 附魔锋利→打出一张攻击牌→看附魔是否还在；查一次性附魔的生命周期 |
| 51 | needs_info | 平板端故事模式右侧三张牌点不了 | 需要设备型号/浏览器；先按触控布局与 `pointer` 事件排查 |

## B. 待你决定的建议（7 条）

以下每条我需要你给"采纳 / 不采纳 / 先放着"，我再排进计划。

| ID | 标题 | 内容摘要 | 工作量 | 我的看法 |
| --- | --- | --- | --- | --- |
| 70 | 增加反制响应时间 | 现 5s，建议 7–10s | 小（改一个常量） | 建议采纳（可做成设置项） |
| 66 | 蜘蛛油巴 UI 太恐怖 | 希望改得不那么恐怖 | 中（要重画/替换素材） | 待你定，属美术决策 |
| 58 | 魔法羽毛/傀儡架台的描述 | 以"特殊效果"形式展现惩罚 | 小 | ✅ **已采纳并完成**（2026-09-11） |
| 57 | 减少水蛭回血 | 现在过强，开局就可能出现 | 小（改数值） | 建议采纳，具体数值你定 |
| 34 | 天梯 2v2 举报结算 | 举报成功→归还花阶分，被举报者判负、其余 3 人判胜 | 中（天梯结算逻辑） | 待你定（涉及赛季分规则） |
| 21 | 天赋投稿「锯齿加护」 | digger boss 专属天赋：每回合 1 层反射＋1 层虚弱，花朵加锯齿边 | 中（新天赋＋美术） | 待你定 |
| 19 | 事件投稿「兄弟，走错游戏了」 | digger 事件：变土 / 抢劫打 boss / 离开，含新卡「土」 | 中（新事件＋新卡） | 待你定 |

## C. 表格14：故事模式待办（17 处标记）

**审查方法**：每个标记项先在代码里定位实现，再逐字比对表格值 →
分三类：✅已实现 / ⚠部分实现 / ❌未实现；只对后两类动手。

| sheet | 标记内容 | 代码位置（已定位） | 现状初判 |
| --- | --- | --- | --- |
| 爬塔卡牌设计 | Magic Feather：回复手牌数/2E（上取整）＋1 层"无法抽牌" | `story_content.py:2089` | 待比对效果文本 |
| 爬塔卡牌设计 | Nitro 类型 → `T` | 同上（`nitro`） | 待比对 |
| 爬塔卡牌设计 | Puppeter：抽 3 张＋获得 -2 层"下回合抽牌"（升级 -1） | `story_content.py:2342` | 待比对 |
| 爬塔卡牌设计 | 红字：25 处"类型"、5 处"标签" | 同名卡 | 批量比对 |
| 爬塔天赋设计 | 认知偏差：回合回复 E＋2、结束 -1（最少 1） | `story_content.py:2715` | ✅ 代码已是该文案 |
| 爬塔天赋设计 | 灵巧：随机 1 张手牌暂时迅捷 1，回合结束移除 | `story_content.py:2734` | ✅ 代码已是该文案 |
| 爬塔天赋设计 | 传染：获取时选 1 张牌获得【传染】…（BOSS 仅 1 层、稀有度 BOSS） | `story_content.py:370` | 待比对 |
| 爬塔天赋设计 | 裂解：选择一张攻击牌永久获得广域打击 | `story_engine.py:9749` | 待比对 |
| 爬塔事件设计 | 邪术师：祈求更大力量→获得标记＋卡组加入 1 张惊吓 | `story_content.py:112` | 待比对 |
| 爬塔事件设计 | 天平的审判：天平倾向你→降级所有牌、翻倍最大生命、获得 1 张疲劳 | `story_events_12.py:345` | 待比对 |
| 爬塔事件设计 | 天平的审判：平衡→随机降级一张牌、回复 20H | 同上 | 待比对 |
| 爬塔怪物设计 | 白蚁丘：灵能绑定/护巢本能整套重写 | `story_content.py:823` | 代码已有近似文案，待逐条比对 |
| 战斗列表 | 工厂精英＝砖堆×1＋机械老鼠 | 遭遇表 | 待比对（对应反馈 #73） |
| 战斗列表 | 砖堆获得"掩体"：被击中使机械老鼠失去隐形、上限×4、不行动 | 同上 | 待比对 |

### C2. 初审结果（2026-09-11 与代码逐项比对）

| 表格标记项 | 代码现状（位置） | 结论 |
| --- | --- | --- |
| 魔法羽毛：回复手牌数/2E（上取整）＋无法抽牌 | `story_content.py:2089`「回复自己等同于当前手牌数向上取整一半的E；本回合无法再抽牌。」 | ✅ 已实现 |
| 傀儡架台：抽 3 张＋下回合少抽 2（升级少抽 1） | `story_content.py:2342`「抽3张牌；下回合少抽2张牌」＋升级文案 | ✅ 已实现（措辞不同） |
| 认知偏差：+2E／结束时 -1（最低 1） | `story_content.py:2715`「每回合多回复2E；回合结束时使每回合E-1，最低为1E。」 | ✅ 已实现 |
| 灵巧：随机 1 张手牌暂时迅捷 1、回合结束移除 | `story_content.py:2734` 同文案 | ✅ 已实现 |
| 传染（BOSS 遗物：获取时选 1 张牌获得【传染】，BOSS 仅 1 层） | 代码里只有 `infect` **类型/标签**（`story_content.py:201/369`），没有同名遗物 | ❌ 未实现（待确认是遗物还是标签改写） |
| 裂解（遗物：选一张攻击牌永久获得广域打击） | `story_engine.py:9749` 有「裂解只能选择攻击牌」校验；`story_events_12.py:31` 有「裂解机」事件 | ⚠ 逻辑部分存在，遗物定义与"永久"范围待比对 |
| 天平的审判·倾向你 | 表格：降级所有牌＋翻倍最大生命＋**1 张**疲劳；代码 `story_events_12.py:352`：**2 张**疲劳 | ⚠ 数值不符（表格要 1 张） |
| 天平的审判·平衡 | 表格：随机降级一张牌＋回复 20H；代码第二选项是「升级所有牌＋减半最大生命」 | ❌ 该选项未实现（或表格是新设计） |
| 白蚁丘（灵能源泉/灵能绑定/护巢本能） | 代码 `story_content.py:812-825`：灵能绑定＝白蚁丘存活时 H 不低于 1、降到 1H 眩晕 2 次后回满；灵能源泉＝开战替换＋死亡立刻决意 | ⚠ 与表格新版（H=2 时眩晕 1 回合并清力量、友方在场回满＋狂暴）不一致，需逐条确认 |
| 战斗列表·工厂精英（砖堆×1＋机械老鼠；掩体：被击中使鼠失隐形、上限×4、不行动） | 代码有 `brick_pile`（藏身/隐形，`move_order=(0,)`）与 `mechanical_rat` | ⚠ 部分：藏身/隐形在，是否有"上限×4"待比对遭遇配置 |
| 邪术师（事件：祈求更大力量→获得标记＋卡组加入 1 张惊吓） | `story_content.py:112` `occultist` 角色 `implementation_status: 'planned'` | ❌ 未实现 |
| 爬塔卡牌设计红字：25 处"类型"、5 处"标签" | 同名卡的类型/标签列 | ⏳ 待批量比对 |

## D. 表格14：全模组卡牌 82 处红标

这 82 处是**多人模式卡牌的平衡改动**（例：Coffee 改"回复 2E＋1 层沉重"、Heavy 改 36D 自眩晕、
Shovel/Missile 费用 1→3、Soil 改"上限+40 并加 5 张灰尘"…）。

处理方式与故事项不同，建议：

1. 用表格与 `mods/*.gtnmod` 的卡数据**逐字段 diff**（费用/类型/标签/效果描述），
   产出"表格 vs 线上包"的差异表；
2. 差异项按"数值平衡 / 描述文案 / 机制改动"分类；
3. 机制类先确认实现方式（是改卡数据还是改引擎原子），再批量落地；
4. 落地后跑编辑器覆盖率报告 + 全套测试，再走部署（重启）。

## E. 执行顺序（建议）

1. **先修可复现、影响面小的漏洞**：#71 扇子回费、#52 一次性附魔、#64 日志翻译、#63 核弹威力、#68 萤火虫刷新
   → 都属"定位清楚、改动小"，先做能立刻降低线上噪音。
2. **再处理故事模式结构性 bug**：#76 设置打不开、#74 附魔 ygg 不触发、#51 平板点击、#60 偶发选牌提示
   → 需要复现环境（#60/#51 还缺信息，先补问作者）。
3. **接着做表格14 的故事项（C 表）**：逐项比对 → 未实现项直接改；
   顺带覆盖 #73（战斗列表）。
4. **最后做全模组卡牌 82 处（D）**：生成差异表 → 分批改 → 覆盖率与测试回归。
5. 每条改完在反馈中心回写状态与 `fix_version`（`/api/feedback/handling/reports/<id>/resolve`）。

## F. 决定与进度

* **建议**（2026-09-11 用户决定）：**只做 #58**（魔法羽毛/傀儡架台的描述改为"特殊效果"写法），
  #70 / #66 / #57 / #34 / #21 / #19 **暂不考虑**。
* **#58 已落地**：
  * `story_content.py` 新增两个"特殊效果"术语：**无法抽牌**（本回合无法抽牌，回合结束时失去）、
    **下回合抽牌**（下个回合开始时多抽/少抽效果层数的牌，随后失去）；
  * 卡面描述改为：魔法羽毛「回复自己等同于当前手牌数一半（向上取整）的E；获得1层无法抽牌」，
    傀儡架台「抽3张牌；获得2层下回合抽牌」（升级 1 层），英文字段同步；
  * 新增术语图标 `static/assets/story-trait-icons/cannot-draw.svg`、`next-turn-draw.svg`
    （测试要求每个术语都有独立配图）；`pytest -k story` **782 passed**。
* 待你回答：**#60 / #51 缺复现信息**，要不要我用管理员账号在反馈中心留言向作者索要（设备/录像）？

## G. 执行进度（2026-09-11 第二批）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 反馈 #58（建议） | ✅ 完成 | 术语 + 图标 + 卡面文案；反馈中心已置 `accepted`，`fix_version=2dcb9d0` |
| 反馈 #64（漏洞） | ✅ 完成 | `mod_runtime_v2._status_label` 补 `magic_nazar → 魔法邪眼`；反馈中心已置 **`in_progress`（修复中）**，`fix_version=77ba231` |
| 表格14 天平的审判 | ✅ 完成 | 疲劳 2 张 → **1 张**（`story_events_12.py`） |
| 反馈 #63 核弹不受威力加成 | ⚠ 实测已生效 | 复现：3E、威力 0 → 27 伤害（9×3）；威力 5 → **42**（14×3），威力按段生效。需要向报告者确认他遇到的具体场景（是否为多人模式核弹/伤害预测显示） |
| 反馈 #71 扇子不回费 | ⏳ 待继续 | 无头复现里 `void:fan` 的 `gain_e target=play_targets` 对**任意目标**都没回费，需要顺着目标解析（`_resolve_step_targets` 的 `play_targets` 分支）继续查 |
| 其余 10 条漏洞 | ⏳ 待做 | 见 A 表；#60/#51 仍缺复现信息 |
| 表格14 其余故事项 | ⏳ 待做 | 白蚁丘机制、传染遗物、裂解"永久"范围、邪术师事件、25 类型 + 5 标签红字、战斗列表工厂精英细节、全模组 82 处 |

## H. 执行进度（2026-09-11 第三批）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 反馈 #76 故事模式设置打不开 | ✅ 完成并实测 | **根因：`templates/story.html` 里 `#story-settings-dialog` 被误放在 `{% if story_coop_access %}` 块内**（该块只对 staff/admin 渲染）。普通账号点 HUD「设置」时 `openStorySettings()` 取不到元素、静默 return，所以"怎么点都没反应"——与#76 评论里 dev 所说的"其实是权限问题"完全一致。已把该 dialog 移出权限块（`templates/story.html`），新增契约测试 `test_story_settings_dialog_is_not_gated_behind_coop_access`（同时约束协作权限块里只能出现 `story-coop*` 元素）。本地起实例 + Playwright 实测：`story_coop_access=false` 的账号下 dialog 存在、点击后 `open=true`、确定后 `story-hide-card-borders`/`story-speed-2x` 生效并写入 localStorage。 |
| 反馈 #68 灌木丛萤火虫 | ✅ 完成（`cf8ed8d`，此前一轮方向理解有误已更正） | 反馈原话是"**灌木丛每回合一定刷出萤火虫**"，指的是精英怪「灌木」而不是遭遇池。真因：`_next_enemy_move` 的 bush 分支用 `spawned + planned >= 3 → 改为养分`，而"吸引"一次召 2 只，只要场上有 1 只召唤物就永远被顶成养分 → 玩家只见过抖落召出的萤火虫（线上 `story_runs` 实查：一条旅程 6 只萤火虫、0 只苍蝇/叶虫）。改为"**超过**上限(3)才改招"，1-2-3 循环恢复为 抖落(1 只萤火虫)→吸引(苍蝇+叶虫，共 3 只)→养分；召唤物上限仍为 3。**上一批按"遭遇池"方向做的改动已回退**（表格 R29 就是"简单怪随机取 3 条 + 抽过不重复"，丛林简单怪恰好 3 条所以必含萤火虫，属设计），改用严格轮换袋并修掉旧实现里"转困难池会重置进度"的缺陷，见 `tests/test_story_jungle_factory.py`。 |
| 白蚁族「决意」补无敌 | ✅ 完成（`95df61c`，表格14 R49-R51 补充） | 表格写"决意：**这回合无敌**XX D击杀自己"，代码只有伤害+自毁。三种白蚁（20D/16D/23D）现在都先给自己 1 回合无敌（`gain_status immediate=True`，因为原有的 enemy 自增益是延迟到回合结束才应用的，决意随后就自毁会等于没加），再结算伤害、自毁。同批核对了白蚁族其余信息（啃咬/头锤/振翅/鼓舞/护卫/狂暴/心神震慑/灵能爆发数值、1-2-3/1-2 循环、白蚁丘 291/308 与固守 12S/15S、号令+1 力量、灵能链接均摊），均已一致。 |
| 反馈 #73 机械老鼠战斗列表 | ✅ 完成 | `story_content.py` 工厂精英改为 砖堆×1（`health_multiplier: 4`）＋机械老鼠；掩体机制（被击中解除隐形、砖堆不行动／显示"掩护"、鼠回合末藏入砖堆）原本已实现。测试见 `tests/test_story_factory_v6.py`。 |
| 反馈 #72 经典 UI 无法触发装备 | ✅ 完成（与 ui_bugs 代理重复覆盖） | 根因在 `static/js/game.js` `attachClassicEquipmentPreviews`：只在首次渲染时（`is-triggerable` 成立）绑定 click/keydown 并打 `dataset.equipmentBound`，而经典 UI 的 chip 会按 `instance_id` 复用 → 之后永远不再绑定。已改为每次渲染都绑定、点击时再校验。 |
| 表格14 天平的审判 | ✅ 完成 | 新增第三选项「平衡」（随机降级一张牌 + 回复 20H，`story_events_12.py` + `story_engine.py` 新 hint `scale_balance`）；并修正 77ba231 的遗留——当时只改了事件**文案**（2 张→1 张），引擎仍发 2 张疲劳，现已改为 1 张。测试：`tests/test_story_events_12.py`（含新 3 条）。 |
| 表格14 全模组卡牌复核 | ✅ 复核完成（0 处需改） | 见 `docs/表格14差异清单.md`；其中"熔岩缺 `self_target`/`wide_strike`、西兰花缺 `sewers:confusion`"是**误报**：`mod_loader.py:377` 会把 `tags` 并入 `flags`，运行时实测 `Lava flags=['self_target','wide_strike']`、`Broccoli flags=['mark:sewers:broccoli','sewers:confusion']`，两张牌都已带齐玩法标签。剩余 2 条属待你决策的**设计**问题（熔岩是否要自伤、魔法珍珠成长条款去留），不是数据缺失。 |
| 表格14 待你决策项 | ⏳ | ① 熔岩是否恢复"自刃"（自伤）；② 传染改 BOSS 池 + 仅限 1 层；③ 白蚁丘整套重写（最大生命减半／H=2 触发／眩晕 1 并清力量／复活狂暴／死亡后改写敌方意图）；④ 魔法珍珠成长条款归属；⑤ 眩晕卡是否补"失明增加效果：所有牌不可取消"文案（引擎已实现该规则）。 |

## I. 执行进度（2026-09-11 第四批：**全部以表格为准**）

用户决定：表14 的待决项**全部按表格执行**（不再逐条确认）。据此：

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 表格14 R170 魔法珍珠 | ✅ 完成（`6e86aaa`） | 去掉"本局每打出过 1 次就额外打出 1 张"成长条款；目标改为**本次所选目标**（`queue_auto_play target=target`）；引擎新增 `dedupe` —— 同一张牌只保留一个自动打出队列项（重复打出不叠加）。四语言 effect_text + locales 全同步。 |
| 表格14 R123 眩晕 | ✅ 完成（`fba7af3`） | 卡面补"（失明增加效果：所有牌不可取消）"并把"每张装备在目标上的**此牌**"改成"**眩晕**"；规则引擎本就实现（`_player_choices_are_uncancellable`），四语言同步。 |
| 表格14 R163 冥界闪电（反馈 #81） | ✅ 完成（`7e8d4c3`） | 表格写"随机造成 7/9 电伤×3"，但卡是 thorn、默认 `target='enemy'`，会要求玩家选一个随后被随机忽略的目标。改为显式 `target='random'`（不走选目标流程），内容校验白名单同步允许 `random`。 |
| 建议 #82 天梯/娱乐分开存模组 | ✅ 完成（`732d466` + `cabc611`） | 新增 `gtn_disabled_mods_ranked`；设置面板/登录/数据请求按模式取键；切模式前先把目标模式那份推给服务端（`set_mode` 用它重建 loadout）。反馈中心已置 accepted + fix_version。 |
| 表格14 R59 传染 | ✅ 完成（`8e67124`） | 稀有度 common→special 并加入 `STORY_BOSS_RELIC_IDS`（只从 BOSS 三选一出）；新增 `STORY_SINGLE_LAYER_RELIC_IDS`，BOSS 三选一与 `_gain_relic` 双重去重，整局最多 1 层。 |
| 表格14 R64 白蚁丘 | ✅ 完成（`a397444`） | 灵能绑定：最大生命减半（开战转换时结算）/ 阈值改 H≤2、眩晕 1 回合并清空力量 / 眩晕结束回满 H 并获得白蚁丘狂暴层数的力量；护巢本能：被攻击时所有白蚁 +1 暂时力量、任一方死亡白蚁丘 +1 狂暴；白蚁丘死亡后把所有可行动白蚁的意图改写为决意（回合结束时照常结算）。测试：`tests/test_story_jungle_factory.py` 5 条；`pytest -k story` 801 passed。 |
| 表格14 全部条目 | ✅ 收口 | 至此表14 的"待决策"与"待办"项全部按表格落地（传染/白蚁丘/魔法珍珠/眩晕/冥界闪电/天平·平衡/工厂精英；熔岩与西兰花复核为误报无需改动）。 |
| 表格14 R257/R265 熔岩、西兰花 | ✅ 无需改动（复核更正） | 表格要求"广域打击+自刃"与"迷惑"：`mod_loader.py:377` 会把 `tags` 并入 `CardDef.flags`，运行时实测两张牌行为均已生效（`_wide_strike_target_ids(0, Lava) = [0, 1]`、`_is_confusion_card(Broccoli) = True`）。 |
